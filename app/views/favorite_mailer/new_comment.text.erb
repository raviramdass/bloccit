New comment to "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

By: <%= @comment.user.name %>

<%= @comment.body %>

Visit online at: <%= topic-post_url(@ost.topic, @pist, anchor: "comment-#{@comment.id}") %>